#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int R;
    cin >> R;
    cout << fixed;
    cout.precision(6);
    cout << (double)M_PI * R * R << endl;
    cout << (double)2 * R * R << endl;
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    solve();
}