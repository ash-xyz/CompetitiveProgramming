#include <bits/stdc++.h>
using namespace std;

void solve()
{
    string s;
    string s1;

    cin >> s >> s1;

    s.size() >= s1.size() ? cout << "go" : cout << "no";
    cout << endl;
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    solve();
}