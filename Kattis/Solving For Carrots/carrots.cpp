#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int N, C;
    string dp[N];
    cin >> N >> C;
    for (int i = 0; i < N; i++)
    {
        cin >> dp[i];
    }
    cout << C;
}